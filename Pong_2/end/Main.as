package  {	import flash.display.MovieClip;	import flash.events.KeyboardEvent;	import flash.events.MouseEvent;	import flash.geom.Point;	import flash.ui.Keyboard;	import flash.text.TextField;	import flash.text.TextFormat;		public class Main extends MovieClip	{		public var paddle1:Paddle;		public var paddle2:Paddle;		public var ball:Ball;		public var startText:TextField;		public function Main() {						this.startText = showText("Press Any Key to Start", 160, 30 );			// constructor code			this.ball = new Ball(this.stage.stageWidth / 2.0, this.stage.stageHeight / 2.0, 20);			this.addChild(ball);			paddle1 = new Paddle(this.stage,this.ball, 60, 10, this.stage.stageWidth,this.stage.stageHeight, new Point(1,0));			this.addChild(paddle1);						paddle2 = new Paddle(this.stage, this.ball,590,10, this.stage.stageWidth,this.stage.stageHeight, new Point(-1,0), 40, 38);			this.addChild(paddle2);			this.stage.addEventListener(KeyboardEvent.KEY_DOWN, startGame);		}				public function startGame(e:KeyboardEvent):void {						this.stage.removeEventListener(KeyboardEvent.KEY_DOWN, startGame);			this.stage.removeChild(this.startText);			this.ball.init();			var r1 = 1.0 - Math.random()*2;			var initDir:Point = new Point(r1,1.0-r1);			//initDir.normalize(1.0);			this.ball.pushBall(initDir);		}		public function showText(text_data:String, w:Number, h:Number):TextField {			var tf:TextField = new TextField();			var myFormat:TextFormat = new TextFormat();			myFormat.size = 16;			tf.text = text_data;			tf.defaultTextFormat = myFormat;			tf.x = this.stage.stageWidth/2.0 - w/2.0;			tf.y = this.stage.stageHeight/2.0 - h/2.0;			tf.width = w;			tf.height = h;			this.stage.addChild(tf);			return tf;		}	}	}